# Database Seeding

Create seed data for local development and testing.

Example seed flow:
1. Create a default team.
2. Create sample users (editor, writer, designer).
3. Create sample tasks with various statuses.

For Prisma, add a `prisma/seed.ts` file and run `npx prisma db seed`.
