# Middleware Logging

Middleware should log important events and errors.

Consider:
- Request logging (method, path, status, response time)
- Error logging with stack traces
- Optional structured logging (JSON) for later analysis
