# Database Security

Protect sensitive data and follow best practices:

- Do not store passwords in plain text (use hashing like bcrypt).
- Use parameterized queries/ORM to avoid SQL injection.
- Implement role-based access (team membership checks) in middleware.
