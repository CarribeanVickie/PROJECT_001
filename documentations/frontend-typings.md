# Frontend Type Definitions

Centralized TypeScript types for shared data models:

- `User` (id, name, email, role, teamId)
- `Task` (id, title, description, type, priority, status, dueAt)
- `Comment` (id, taskId, authorId, text, createdAt)

Keep types in sync with backend models for consistent API contracts.
