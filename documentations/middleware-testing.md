# Middleware Testing

Recommended tests:

- Unit tests for controllers and services (mock database calls)
- Integration tests for route handlers (supertest against Express app)
- Validation tests (schema correctness)

Use Jest or Vitest for test runner and mocking.
